#include "main.h"

/**
 * _isupper -  checks for uppercase character
 * @c: is an integer
 * Return: 1 if uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 41 && c <= 90}
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
